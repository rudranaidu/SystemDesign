Gateway : It provides connecting point(secure interface) to the outside world to the internal services.
Routing external requests  to internal services
when you are hitting domain.com in the URL , you are actually hitting hte gateway.
BLock list APIs, It provides RateLimiting capabilities

Authentication and Global caching:

Reasons for KAFKA performance:
     KAFKA is distributed messaging system. 
     It uses sequential accesss and Zero copy principle (Direct Memory access)-> it makes use of OS cache directly

